import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: { main: "#305f7a" },
    secondary: { main: "#ae0015" },
    // error: { main: "#ea9c00" }
  }
});

export default theme;
